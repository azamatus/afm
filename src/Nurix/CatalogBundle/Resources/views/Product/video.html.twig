<div>
    {% for video in product.videos %}
    <div class="char_title">{{ video.title }}</div>
    <span class="video_desc">{{ video.description }}</span>
    <div><iframe width="560" height="315" src="http://www.youtube.com/embed/{{ video.videoId }}" frameborder="0" allowfullscreen></iframe></div>
    {% endfor %}
</div>